<div class="user-info">
	<%= image_tag @user.avatar.url(:medium) %>
	<h1><%= @user.name %></h1>
	<% if current_user == @user %>
		<%= link_to "Settings", edit_user_registration_path %>
	<% end %>
</div>